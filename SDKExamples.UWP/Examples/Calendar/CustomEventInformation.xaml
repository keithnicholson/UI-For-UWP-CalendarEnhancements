<local:ExamplePageBase
    x:Class="SDKExamples.UWP.Calendar.CustomEventInformation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SDKExamples.UWP"
    xmlns:localCalendar="using:SDKExamples.UWP.Calendar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:input="using:Telerik.UI.Xaml.Controls.Input"
    mc:Ignorable="d"  x:Name="page" >

    <Page.DataContext>
        <localCalendar:ViewModelCalendarEvents />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.Resources>
            <localCalendar:CustomCalendarCellStyleSelector x:Key="selector" CellTemplate="{StaticResource cellTemplate}" />
            <localCalendar:CellModelToEventConverter x:Key="eventConverter" />

            <DataTemplate x:Key="cellTemplate">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="⏰" Foreground="#FF3399FF" FontSize="15" HorizontalAlignment="Right" FontFamily="Segoe UI Symbol" Margin="0,1,2,0" />
                    <StackPanel Grid.Row="1"  VerticalAlignment="Top"
                          ScrollViewer.VerticalScrollBarVisibility="Visible"
                          ScrollViewer.VerticalScrollMode="Enabled"
                          ScrollViewer.IsVerticalRailEnabled="True">
                        <TextBlock Grid.Row="1" Text="{Binding Converter={StaticResource eventConverter}}" Foreground="#FF767676" FontSize="13.333" VerticalAlignment="Bottom" Margin="6,0,0,4" />
                        <ListView x:Name="MyView"
                                  ItemTemplate ="{StaticResource MessageViewTemplate}"
                                >
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsStackPanel ItemsUpdatingScrollMode="KeepLastItemInView" />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListViewItem Content="A"/>
                            <ListViewItem Content="B"/>
                            <ListViewItem Content="C"/>
                        </ListView>

                    </StackPanel>
                </Grid>
            </DataTemplate>
            <DataTemplate x:Key="MessageViewTemplate" x:DataType="x:String">
                <Grid Height="Auto" Margin="4">
                    <StackPanel MinHeight="5" Width="25" 
                                                Padding="0,0,0,0"
                                                Background="CadetBlue"
                                                CornerRadius="5">
                        <TextBlock Text="{Binding ElementName=ListViewItem}"/>
                    </StackPanel>
                </Grid>
            </DataTemplate>

        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title, ElementName=page}" Style="{StaticResource ExampleHeaderTextBlockStyle}" />
        <input:RadCalendar CellStyleSelector="{StaticResource selector}" Grid.Row="1"/>
    </Grid>
</local:ExamplePageBase>
